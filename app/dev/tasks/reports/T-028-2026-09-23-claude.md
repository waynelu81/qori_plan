---
task: T-028
owner: claude
date: 2026-09-23
outcome: done
gate: 1130 tests, 5574 assertions
---

# T-028 — report

## Outcome

Connecting Stripe from a Series' price, or Google Drive from its Episode form,
now brings the creator back to that Series instead of leaving them on
Integrations. Both links still go to Integrations, where Google's tiers and
Stripe's country are chosen, but now name the Series, the control that asked
and, for Drive, the kind the form was on; Integrations turns that into the
forwarding address each vendor landing already spends first and once. A file
Episode starts on Qori storage while Drive is not connected, so a new
creator's first file needs nothing connected, and on Drive once it is.

## Verified

| What                             | How | Result |
| -------------------------------- | --- | ------ |
| `composer ci:check`              | Steps one by one, the suite at `HEAD` with `T-190` and `T-191` | Green: 1,130 tests, 5,574 assertions, on `qori_wt2_testing`. A first run on `qori_wt3_testing` errored in 59 tests with missing tables: another suite shared that database. One risky, the one `T-155` reported |
| The change actually did something — how | Before: both links were the bare Integrations URL (`SeriesPriceTest` pinned it and failed after the change), and the file form opened on Google Drive with "Connect your Google Drive first" | Reproduced, then fixed |
| State-specific browser walk      | A throwaway creator with one draft Series and no connections, signed in by `qori:magic-link`: the file form opened on Qori storage; switched to Drive, its Connect link carried `series`, `control=new-episode`, `kind=file`; a price showed Connect Stripe with `control=series-details`; Integrations rendered from the link; `?kind=video#new-episode` reopened the form on Video, scrolled to it | Pass |

**Could not verify:** a real round trip through Stripe or Google, which needs
the owner's sign-in at each vendor. The return is proven by the Stripe decline
landing in `SeriesReturnTest`, which spends the address the same way a
connection does, and by the existing Google landing tests that spend
`ConnectionsDestination`. The walk user was deleted afterwards.

## Where the specification was wrong

| Section | Said | Actually |
| ------- | ---- | -------- |
| Files   | Five files | `tests/Feature/Series/SeriesPriceTest.php` pinned the old `paymentsUrl` and was updated (Added during execution). |

## Departures

- `SeriesController::driveProps()` takes the `Series`, as the Code section
  said; its one caller passes it.
- The `kind` query now decides the Episode form whenever the page opens, and
  the last Episode after each save; `T-026` had applied it only while the
  Series had no Episodes. This is the spec's own decision, noted because it
  changes `T-026`'s code.

## Found, not fixed

- Stopping my own earlier suite run with `pkill -f "artisan test"` would also
  have stopped any other session's suite on this machine. → decided: not a
  task — reported to the owner; stop a run by its task id

## Evidence

None saved.

## Files

**Created:** `tests/Feature/Series/SeriesReturnTest.php`.

**Modified:** `app/Http/Controllers/Share/IntegrationsController.php`,
`app/Http/Controllers/Share/SeriesController.php`,
`resources/js/pages/share/series/Show.vue`, `docs/flows/billing.md`,
`docs/flows/storage.md`, `tests/Feature/Series/SeriesPriceTest.php`. In
`qori-plan`: this task and its report.
