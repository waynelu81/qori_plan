---
task: T-053
owner: claude
date: 2026-09-13
outcome: done
gate: 617 tests, 2977 assertions
---

# T-053 — report

## Outcome

Signing out now lands on the sign-in page, which says "You're signed out."
once, above the method chooser, and says nothing on the next visit. It used to
land on the marketing page. The sentence travels under its own flash key, so the
sign-in page's rule that `status` is shown only after a link was requested from
that page is untouched. Staff sign-out is untouched.

## Verified

| What                       | How                                                                               | Result                                                                                                                                                            |
| -------------------------- | --------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `composer ci:check`        | Clean tree, `phpstan clear-result-cache` first                                    | Green: 617 tests, 2977 assertions. The one failure on that run was the board, stale because this task was still `doing`; regenerated, planning tests re-run green |
| The defect actually failed | `LogoutResponse` binding commented out, case 1 re-run                             | Fails, landing on `/`                                                                                                                                             |
| Browser, dark, by eye      | Dev server, signed in as the seeded creator, sign-out posted, sign-in page opened | "You're signed out." under the description, above the tabs                                                                                                        |
| Browser, light             | Not run                                                                           | —                                                                                                                                                                 |
| Mobile width (360px)       | Not run                                                                           | —                                                                                                                                                                 |

**Could not verify:** the sentence's placement in the light theme and at
narrow width. It is one muted paragraph in a centred column that already holds
several; the risk is a line break, not a break.

## Where the specification was wrong

None. The trace of `destroy()` invalidating before the response is built was
read from the framework and held: the flash landed in the fresh session and was
gone on the second request, which case 2 asserts.

## Departures

None.

## Found, not fixed

**There is no visible sign-out control on the receiving home or a Group
dashboard that a page search finds.** The browser walk had to post the form by
hand. It is presumably in the user menu, but a control that a text search for
"Log out" cannot find on two pages is worth a look in the next design pass.

## Evidence

**No file kept.** What the page showed after the sign-out redirect: the lockup,
"Log in to Qori", "Choose how you would like to sign in", then "You're signed
out." in the muted colour, then the Password / Email link chooser and the form.
The DOM text confirms the same order.

## Files

**Created**

- `app/Http/Responses/LogoutResponse.php`
- `tests/Feature/Auth/SignOutTest.php`

**Modified**

- `app/Providers/FortifyServiceProvider.php`
- `resources/js/pages/auth/Login.vue`
- `lang/en/auth.php`
- `tests/Feature/Auth/AuthenticationTest.php`
