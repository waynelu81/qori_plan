---
task: T-085
owner: claude
date: 2026-09-22
outcome: done
gate: 978 tests, 4721 assertions
---

# T-085 — report

## Outcome

A priced Series whose Group holds no payment account no longer offers to sell
itself. The public page shows one sentence naming the Group — "Harbour Lane
Studio isn't taking payments for this Series yet. Let them know you'd like
it." — in place of the name-and-email form, the code step and the "Get access"
button, so nobody fills a form that ends in a refusal. An account that exists
is offered as before, as the owner decided. And a refusal the server flashes
at a page without the app shell now appears: those pages had no Toaster, so
every toast sent to the public pages, setup, sign-in and the console reached
nobody — including `T-164`'s note after connecting Stripe in setup.

## Verified

| What | How | Result |
| --- | --- | --- |
| `composer ci:check` | From a clean tree, after the code commit | Pint, the frontend checks, vue-tsc, PHPStan and 978 tests, 4721 assertions, pass (1 risky, the existing one in the rule tests) |
| The defect actually failed — how | The 21 September walk (`walkthroughs.md`), and the code: `app.ts` gave `public/*` and setup pages no layout, and only `AppSidebarLayout` mounted `<Toaster />` | "Get access — A$20.00" posted, the server flashed `unsupported_operation`, and the page showed nothing |
| State-specific browser walk | Built-in browser, signed in as Rita, on `harbour-lane-studio`'s priced "The Quiet Handover" with its account cleared for the walk | The page showed the price, then the sentence in an info notice, and no form or button. With the account put back (the one the purge deleted, so Stripe refuses it), ticking the box and pressing "Get access — A$89.00" showed an error toast bottom right, where before it would have shown nothing |
| The consent box | Same page | "I agree that Harbour Lane Studio can email me about this Series." |

**Could not verify:** the stranger's view in a browser — the walk was signed
in; the sentence stands in for the stranger's form by the same condition, and
the prop is tested. A toast on a setup page after connecting Stripe was not
walked: it needs Stripe's sign-in, which is the owner's.

## Where the specification was wrong

- **The draft said the buyer "reaches … a refusal after the click".** They
  reached nothing visible: no Toaster on a page without the app shell. The
  Toaster on every layout joined the scope (Re-scope log), because the
  refusal still answers a page loaded before the account went.
- **Its third bullet, the creator's Series page**, was done by `T-054`: the
  price field has said "Nobody can pay for this :series until you connect
  Stripe", with a Connect link, since then.

## Departures

- The console's layout got a Toaster too, so that every layout has one and
  `docs/architecture/errors.md` can say so.
- Two `fixups.md` lines fixed in the lang lines this touched, and deleted
  there: the consent box and the checkout refusal said "series" in lowercase.
  `free_series` and `already_granted`, in the same block, had the same slip and
  were fixed with it; the three refusals now carry the Group's nouns from
  `CheckoutService`, and `Connect`'s own `not_connected` does too.

## Found, not fixed

- Continue on the public page stays disabled until the consent box is ticked,
  with no hint why (`fixups.md`). → decided: it waits on the owner's answer to
  whether free access needs consent at all, asked on the first-share journey
- The deleted account behind `harbour-lane-studio` makes Stripe refuse its
  checkout with "A service Qori relies on isn't responding right now", which
  blames Qori for a creator's account. → T-165

## Evidence

Screenshots were taken in the built-in browser and not kept: the public page
with the notice and no form; the same page with the account back and the
error toast after pressing "Get access".

## Files

**Created:** `resources/js/layouts/BareLayout.vue`.

**Modified:** `app/Http/Controllers/PublicSeriesController.php`,
`resources/js/pages/public/Series.vue`, `lang/en/accesses.php`,
`lang/en/errors.php`, `app/Services/CheckoutService.php`,
`app/Integrations/Stripe/Connect.php`, `resources/js/app.ts`,
`resources/js/layouts/AuthLayout.vue`, `resources/js/layouts/AdminLayout.vue`,
`docs/flows/checkout.md`, `docs/architecture/errors.md`,
`tests/Feature/PublicSeriesTest.php`,
`tests/Feature/Access/SeriesAccessContinueTest.php`.
