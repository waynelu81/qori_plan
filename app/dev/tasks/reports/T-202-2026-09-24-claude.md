---
task: T-202
owner: claude
date: 2026-09-24
outcome: done
gate: 1146 tests, 5698 assertions
---

# T-202 — report

## Outcome

The Series page's "Have a password? Sign in instead." now remembers its Series
as a path, `/s/{group}/{series}`, in both `url.intended` and
`SignInDestination`. Before, it stored an absolute URL on whatever host the
page was reached on, and `SignInDestination` silently refused any host that
is not `APP_URL`'s, so on a second host the verification wall could not name
the Series, verifying did not return to it, and `T-196`'s register page asked
"What brings you here?" and made a Group. Now all three hold on any host. A
destination pointing off the application is still refused.

## Verified

| What                             | How | Result |
| -------------------------------- | --- | ------ |
| `composer ci:check`              | Steps one by one: `vp check --fix`, `pint --test`, `vue-tsc --noEmit`, PHPStan, and the suite on `qori_wt1_testing` | Green: 1,146 tests, 5,698 assertions. One risky, the one `T-155` reported |
| The change actually did something — how | The three test files against the controller before the change (`git stash` of `PublicSeriesController.php`) | 4 failed: the new signing-up case made a Group ("Failed asserting that true is false"), the new host case and the two pinned assertions found the absolute URL or nothing |
| State-specific browser walk      | The dev server on `127.0.0.1:8001` with `APP_URL=http://localhost:8001` — the host the `T-196` walk failed on — at desktop width: the Series page's sign-in link, Sign up: "You're making an account to get Reading a Room Before You Speak." Registered: back on the Series, no Group and no collaboration in the database; the verification wall said "we'll take you straight to Reading a Room Before You Speak"; the email's link returned to the Series on the same host | Pass. The walk account was deleted afterwards |
| `php artisan qori:e2e`           | `--database=qori_wt1_testing --viewport=both`, because it touches the sign-in return | 9 passed, 1 skipped (first-share runs desktop only, by design), 4.6 minutes |

**Could not verify:** a real second production hostname; whether production
has one is the owner's to say. The walk also covered `T-196`'s register page
at desktop width, which that report could not.

## Where the specification was wrong

None. The draft's one open bullet was answered from the code when it was
brought to ready (its Re-scope log).

## Departures

None. No file outside the Files table was touched.

## Found, not fixed

None.

## Evidence

None saved. A screenshot of the register page naming the Series on
`127.0.0.1` at desktop width was taken in the session's browser pane.

## Files

**Modified:** `app/Http/Controllers/PublicSeriesController.php`,
`docs/flows/auth.md`, `tests/Feature/PublicSeriesTest.php`,
`tests/Feature/Auth/SignupIntentTest.php`,
`tests/Feature/Auth/VerificationDetourTest.php`. In `qori-plan`: this task,
its report, and the onboarding stream's receiving paragraph.
