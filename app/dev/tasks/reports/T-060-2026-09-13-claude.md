---
task: T-060
owner: claude
date: 2026-09-13
outcome: done
gate: 627 tests, 3077 assertions
---

# T-060 — report

## Outcome

The domain's eighteen enums live in `app/Enums`, flat, with their history
intact under `git mv`. `app/Models/Enums` is gone. The namespace is rewritten
in every importer across `app/`, `tests/` and `database/`, the two path-based
reads in `ModelEnumTest` point at the new directory, and the directory-walking
case no longer claims the namespace is "models". `ErrorCode`, `StaffRole` and
`StaffAbility` are where they were, as the contracts of their layers.
`CLAUDE.md` and its Cursor mirror now say where enums live and why those two
stay, and the decision record says the same.

No enum's cases, methods or backing values changed.

## Verified

| What                 | How                                                                                   | Result                                                                                                                                                            |
| -------------------- | ------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `composer ci:check`  | Clean tree, `phpstan clear-result-cache` first, `composer dump-autoload` before tests | Green: 627 tests, 3077 assertions. The one failure on that run was the board, stale because this task was still `doing`; regenerated, planning tests re-run green |
| The grep in the task | Over `app/`, `tests/`, `database/`, `config/`, `CLAUDE.md`, `.cursor/`                | Nothing left                                                                                                                                                      |
| The old directory    | `ls -d app/Models/Enums`                                                              | Gone                                                                                                                                                              |
| History              | `git mv`, not delete-and-add                                                          | Each file's log continues across the move                                                                                                                         |
| Browser              | Not run                                                                               | Nothing rendered changed                                                                                                                                          |

**Could not verify:** nothing this task needed. A namespace move with a green
suite and an empty grep is verified by those two things.

## Where the specification was wrong

| Section                | Said        | Actually                                                                                                                                                                                        |
| ---------------------- | ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Why, Files, Acceptance | "seventeen" | Eighteen. The count was taken from a list read the day before `T-059`, which had not yet made `SignupIntent` the odd one out; the move counted them. Corrected in the task before the gate ran. |

## Departures

**`docs/planning/pricing-and-competitor-review-2026-09-11.md` was updated**
because it names the path as a fact about the code, not as history. Done task
files that name the old path were left alone, as the Scope says.

## Found, not fixed

None.

## Evidence

None needed. The grep, the directory listing and the suite are the evidence,
and all three are reproducible from the task file.

## Files

**Moved**

- `app/Models/Enums/*.php` → `app/Enums/*.php`, eighteen files

**Modified**

- Every importer of the old namespace in `app/`, `tests/` and `database/`
- `tests/Feature/Enums/ModelEnumTest.php`
- `CLAUDE.md`, `.cursor/rules/laravel-conventions.mdc`
- `docs/planning/pricing-and-competitor-review-2026-09-11.md`
- `docs/planning/decisions.md`
