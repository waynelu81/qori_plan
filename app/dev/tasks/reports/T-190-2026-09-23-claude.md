---
task: T-190
owner: claude
date: 2026-09-23
outcome: done
gate: 1130 tests, 5574 assertions
---

# T-190 — report

## Outcome

The home page says who Qori is for. "Start with the people who already ask
you" opens the sentence under the headline, and a second small note under the
buttons says what Qori is not for, that files stay where they are, that what
Peers pay is theirs less Stripe's fee, and that the first Series is free. At
phone width the headline reads "today. Their" with its space (R-004 F-9).

## Verified

| What                             | How | Result |
| -------------------------------- | --- | ------ |
| `composer ci:check`              | Steps one by one, the suite at `HEAD` with `T-191` and `T-028` | Green: 1,130 tests, 5,574 assertions, on `qori_wt2_testing`. A first run on `qori_wt3_testing` errored in 59 tests with missing tables: another suite shared that database. One risky, the one `T-155` reported |
| The change actually did something — how | At 375px before, the headline's `innerText` ran "today.Their"; after, "today. Their" | Reproduced, then fixed |
| State-specific browser walk      | Signed out, at 375px and at the pane's desktop width: the three paragraphs read as specified, no sideways scroll | Pass |

**Could not verify:** light theme; the pane rendered dark.

## Where the specification was wrong

None. The formatter kept `{{ ' ' }}` after the `<br>`, as the Code section
allowed for.

## Departures

A short template comment above the new paragraph saying why no plan number is
written there.

## Found, not fixed

None.

## Evidence

None saved; screenshots at 375px and desktop width in the session's pane.

## Files

**Modified:** `resources/js/pages/Welcome.vue`. In `qori-plan`: this task and
its report.
