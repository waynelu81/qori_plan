---
task: T-007
owner: claude
date: 2026-09-09
outcome: done
gate: 466 tests, 1509 assertions
---

# T-007 — report

## Outcome

Somebody who proves control of their inbox — by receiving a signed, expiring,
single-use link at that exact address and clicking it — is no longer shown a
wall asking them to prove control of their inbox. The proof already happened;
the product just was not recording it.

## Verified

| What                    | How                                  | Result |
| ----------------------- | ------------------------------------ | ------ |
| `composer ci:check`     | Full gate                            | Green  |
| Browser, light and dark | Not applicable — no interface change | —      |
| Mobile width (360px)    | Not applicable                       | —      |

**Could not verify:** nothing. The change is four lines with a test per branch,
and the transition is asserted in both directions within a single test.

## Where the specification was wrong

| Section | Said                                   | Actually                                                                                                               |
| ------- | -------------------------------------- | ---------------------------------------------------------------------------------------------------------------------- |
| Tests   | `tests/Feature/Auth/MagicLinkTest.php` | No such file. The suite calls it `MagicLinkRoutesTest.php`; the spec anticipated this and said to add the cases there. |

## Departures

A fourth test beyond the three specified:
`test_signing_in_with_a_password_does_not_verify_the_address`. The spec put
password sign-in under **Out** and said to "assert or reason about it". A test
is the cheaper of the two, because the tempting next change is to make the
sign-in paths consistent — and consistency here would mean verifying an
address on evidence that does not exist.

## Found, not fixed

Nothing.

## Evidence

None. No interface change.

## Files

**Modified:** `app/Http/Controllers/Auth/MagicLinkLoginController.php`,
`tests/Feature/Auth/MagicLinkRoutesTest.php`
