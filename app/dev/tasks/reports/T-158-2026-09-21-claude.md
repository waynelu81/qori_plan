---
task: T-158
owner: claude
date: 2026-09-21
outcome: done
gate: GATE_PENDING
---

# T-158 — report

## Outcome

Certificates and the hours log are off by default behind
`config('qori.certificates.enabled')` (`QORI_CERTIFICATES`). The public check
page and the Peer's record answer 404, "View your certificate" and "My record"
are gone, and neither Series form asks for hours. A Peer who finishes still
gets a code, so `T-109` switches them back on with nothing lost (`D-044`).

## Verified

| What | How | Result |
| --- | --- | --- |
| `composer ci:check` | From the working tree | See the gate line; the first run failed one test, below |
| The defect actually failed — how | Before the change the walk Peer's Series page and the sidebar offered the certificate and "My record" | Confirmed in the code (`SharedController::show()`, `AppSidebar.vue`) |
| State-specific browser walk | Built-in browser, `localhost:8001`, desktop width | Creator: no Hours field on the new-Series form or the Series page; `/certificates/…` 404. Peer: marked the only Episode done, page shows "Finished", no certificate link, sidebar is Home and Shared with me |
| Minting unchanged | tinker, after the Peer finished in the browser | `completed_at` stamped and `certificate_code` `SP6W-4FJH-1LHF` written |

**Could not verify:** `tests/e2e/public-link.spec.ts` was edited and not run.
`php artisan qori:e2e` empties Mailpit's inbox first, and the Mailpit on
`1025`/`8025` is another project's container, so running it would have deleted
somebody else's mail. Its new assertion was checked by hand against the page
instead, and the first draft of it was wrong — see below.

## Where the specification was wrong

- **The spec's Changed tests missed one.** `tests/Feature/Series/DeleteSeriesTest.php`
  (`test_the_sweep_keeps_the_certificate_code`) opens `/certificates/{code}` to
  prove a purge keeps the code. The grep that wrote the Files table listed it;
  the table did not. The first full gate failed on it with a 404, and it now
  switches certificates on as the other three do.
- **A composable the Files table did not name.** Three components read the
  switch; `resources/js/composables/useFeatures.ts` beside `useTerminology`
  is how this codebase reads shared props. Recorded under **Added during
  execution**.
- **The e2e assertion as first written would have failed.** It expected
  "1 of 1" after the last Episode was marked done; the page says "Finished"
  (`SeriesProgress.vue:44`). Caught in the browser before it was committed.

## Departures

- `tests/Feature/Series/DeleteSeriesTest.php` — edited, above.
- `resources/js/composables/useFeatures.ts` — new, above.

## Found, not fixed

- Lines that mention certificates in passing still do: the deletion copy in
  `lang/en/series.php` and the vocabulary settings page. True of the data,
  and left for whoever decides what a certificate says. → T-109
- The e2e journey no longer reaches a certificate; putting those steps back is
  part of switching them on. → T-109
- What exactly does not work in the current implementation was not recorded:
  the owner did not want to spend the time. → T-109

## Evidence

None committed. The browser checks are described above in words.

## Files

**Created:** `resources/js/composables/useFeatures.ts`,
`tests/Feature/Shared/CertificatesSuspendedTest.php`.

**Modified:** `config/qori.php`, `.env.example`,
`app/Http/Controllers/CertificateController.php`,
`app/Http/Controllers/Shared/SharedController.php`,
`app/Http/Middleware/HandleInertiaRequests.php`,
`app/Console/Commands/DesignReviewCommand.php`,
`resources/js/types/global.d.ts`, `resources/js/components/AppSidebar.vue`,
`resources/js/pages/share/series/Index.vue`,
`resources/js/components/series/SeriesForm.vue`,
`tests/Feature/Shared/CertificateTest.php`,
`tests/Feature/DesignReviewFixtureTest.php`,
`tests/Feature/Series/DeleteSeriesTest.php`, `tests/e2e/public-link.spec.ts`,
`docs/flows/series.md`, `docs/tinker/e2e.md`.
