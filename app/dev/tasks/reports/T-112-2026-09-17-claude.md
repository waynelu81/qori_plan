---
task: T-112
owner: claude
date: 2026-09-17
outcome: done
gate: 789 tests, 4230 assertions
---

# T-112 — report

## Outcome

Stripe's HTTP clients and error decoding are a class,
`App\Integrations\Stripe\Client`, injected into `Connect` and `Subscription`,
with the owner's names: `getClientV1()`, `getClientV2()`, `getClientOAuth()`
and `unwrap()`. `app/Integrations/Concerns` is gone, and `ArchitectureTest`
now checks that it stays gone and that no Service imports a Stripe class.
Nothing a creator or a Peer sees changed. `T-113` has since deleted
`getClientV2()` with its last callers.

## Verified

| What                             | How                                                                                                                                                                                                                                                  | Result                                                  |
| -------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------- |
| `composer ci:check`              | In the main checkout with `T-111` and `T-113` applied, `DB_DATABASE=qori_wt4_testing`, step by step as `T-111`'s report describes; the literal command stopped at `vp check` on another session's untracked planning file                            | All green: 789 tests, 4230 assertions, PHPStan 0 errors |
| The defect actually failed — how | Nothing to reproduce: this is a move. On its own branch, before `T-113`, the Stripe, Connect, Payouts, Subscription, Checkout, StatementDescriptor, Architecture and Billing tests passed unchanged, 135 of 135, with the worktree's own code loaded | Behaviour unchanged                                     |
| State-specific browser walk      | None: no page changes                                                                                                                                                                                                                                | Not applicable                                          |

**Could not verify:** the T-112 commit on its own through the full gate. The
full gate ran once, on all three tasks together; T-112 alone ran the 135
Stripe-related tests and PHPStan.

## Where the specification was wrong

None.

## Departures

None.

## Found, not fixed

- The `fromStripe()` factories on `ConnectAccount`, `HostedCheckout` and
  `GroupSubscription` map Stripe payloads inside `app/Data`, against `D-022`
  → draft T-114
- `StripeWebhookController` still computes Stripe's signature and reads the
  event's fields itself
  → draft T-114
- `StripePurgeConnectedAccountsCommand` builds its own Stripe clients rather
  than using `Client`
  → draft T-114
- A worktree with `vendor` symlinked to the main checkout runs the main
  checkout's code: Composer's optimised classmap and Laravel's base path both
  resolve through the symlink, so `php artisan test` in the worktree tested the
  main checkout's classes, and the new `Client` looked missing, until the
  builder loaded the worktree's classes first
  → decided: the symlink was this session's shortcut, not the repository's instruction; recorded where the next session will read it

## Evidence

None.

## Files

**Created:** `app/Integrations/Stripe/Client.php` (moved from the trait),
`tests/Feature/Integrations/Stripe/ClientTest.php`.

**Modified:** `app/Integrations/Stripe/Connect.php`,
`app/Integrations/Stripe/Subscription.php`, `tests/Feature/ArchitectureTest.php`.

**Deleted:** `app/Integrations/Concerns/StripeHttpClient.php`.
