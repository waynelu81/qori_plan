---
task: T-160
owner: claude
date: 2026-09-21
outcome: done
gate: 921 tests, 4428 assertions
---

# T-160 — report

## Outcome

A Peer pressing Open on a Google Drive Episode is given reader on that one
file, with the creator's token, inside their request, and sent to Drive's
viewer (`D-040`). A granted row is trusted and sends them on with no Google
call; a refusal sends them back to the Series with the reason, and Open again
is the retry. Step 14 of the first-share journey works in code.

## Verified

| What | How | Result |
| --- | --- | --- |
| `composer ci:check` | From the working tree | Pint, PHPStan and 921 tests pass (1 risky, the existing one in the rule tests) |
| The defect actually failed — how | `PlaybackTicketService::open()` before the change | The grant was a comment; a Drive Episode had no resolver at all |
| State-specific browser walk | Built-in browser as the walk Peer, against a Google connection seeded with a fake token, then removed | Open sent Google a real `permissions.create`; Google answered 401 `authError`; the Peer landed back on the Series with `needs_creator`'s toast; the row read `needs_creator`, `authError`, the Peer's address |
| Every state | `OpenDriveEpisodeTest`, Google faked with `T-093`'s bodies | granted and sent on; second Open makes no call; `cannotInviteNonGoogleUser` → `awaiting_identity`; dead connection → `needs_creator` and no call; 500 → `pending`, then granted on the next Open; no access → 403 and no row |

**Could not verify:** a real grant landing on a real file and a Peer opening it
in Drive. That needs a creator's real Google connection and a file picked in
the Picker — the owner has a live connection locally from `T-044`'s walk, and
it is theirs to use — and a second Google account signed in as the Peer. It is
`T-161`'s by-hand walk.

## Where the specification was wrong

- **The toast table missed that an existing test used `pending` for the
  fallback.** `OpenEpisodeTest::test_a_blocked_link_sends_the_peer_back_to_the_series_page_at_its_notice`
  mocked a blocked `pending` link and expected the "what's left is below" line,
  which `pending` no longer falls back to. It now uses `awaiting_acceptance`,
  which has no line of its own, and still tests the fallback.
- **`awaiting_identity`'s line names no vendor**, because `D-035` asks the
  owner first and the answer has not come; the version naming Google sits in a
  comment beside it for a one-line swap.

## Departures

- `tests/Feature/Storage/OpenEpisodeTest.php` — the fallback state, above.

## Found, not fixed

- A Google 401 at Open is `needs_creator`, but nothing marks the connection
  for reconnect, so Integrations still says connected until the daily refresh
  finds out. `T-091` planned exactly this. → T-091
- A permission the creator removes by hand in Drive is never noticed: the
  granted row sends the Peer to Google's "request access" (`D-040` accepts the
  gap; re-checking is `T-091`'s). → T-091
- `completed_at` is sticky, so adding a second Episode after a Peer finished
  the first leaves the Series reading "Finished" at one of two. The rule was
  for certificates, which are suspended. → T-109
- Peer-facing copy needs `D-035`'s answer before `awaiting_identity` can say
  Google. → decided: asked the owner on 21 September 2026; the swap is one line in `lang/en/shared.php`

## Evidence

None committed. The browser check is described above in words.

## Files

**Created:** `database/migrations/2026_09_21_000000_create_vendor_grants_table.php`,
`app/Models/VendorGrant.php`, `app/Enums/VendorGrantStatus.php`,
`app/Enums/GrantTarget.php`, `app/Data/GrantResult.php`,
`app/Integrations/Contracts/GrantsItemAccess.php`,
`app/Integrations/Google/GoogleDriveFiles.php`,
`app/Services/VendorAccessService.php`,
`tests/Feature/Storage/OpenDriveEpisodeTest.php`.

**Modified:** `app/Providers/IntegrationServiceProvider.php`,
`app/Services/PlaybackTicketService.php`,
`app/Http/Controllers/Shared/OpenEpisodeController.php`, `lang/en/shared.php`,
`docs/flows/storage.md`, `tests/Feature/Storage/OpenEpisodeTest.php`.
