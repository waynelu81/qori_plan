---
task: T-120
owner: claude
date: 2026-09-17
outcome: done
gate: 799 tests, 4063 assertions
---

# T-120 — report

## Outcome

`php artisan qori:e2e` now runs the whole core loop, both halves. Besides the
creator's journey from `T-119`, a stranger opens the Series link, asks for a
code, types the six digits from the email that actually went, opens the
Episode, marks it done and reaches their certificate; and a learner registers
with "I want to learn", is given access by the creator from the Series page,
and follows the "You're in" email to the Series. `--pause=3000` holds every
screen for three seconds, which is what the owner asked for after `--slow`
proved too quick to read. The three journeys take about 45 seconds headless.

## Verified

| What                             | How                                                                                                                                                                                                                                                                                                                                          | Result                                                                                          |
| -------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------- |
| `composer ci:check`              | Step by step, as `T-119`: Pint over the tree, `qori:tasks --check`, `vp check --fix` on every file touched, `vue-tsc --noEmit` at the root and with `tests/e2e/tsconfig.json`, PHPStan over the tree, `php artisan test --parallel`. The literal `npm run check` still stops on another session's two untracked files under `docs/planning/` | All green: 799 tests, 4,063 assertions, 2 risky (pre-existing), PHPStan 0 errors, vue-tsc clean |
| The defect actually failed — how | Not a defect; three journeys were added. All three passed on their first run, so nothing here was made to fail first. What `--pause` fixes was observed, not reproduced: the owner watched the `T-119` headed run and could not follow it                                                                                                    | Headless: 3 passed in 43.2 s                                                                    |
| State-specific browser walk      | The runs are the walks. Headless on `qori_e2e`; `--headed --pause=2000`, watched, every screen held (3 passed in 2.0 m against 43 s without); `--viewport=mobile` (3 passed in 47.9 s); `--only=stranger` and `--only=learner` each ran exactly one journey. After each, `lsof` showed nothing on 8009                                       | All passed at every step                                                                        |
| The Peer's code                  | Read from the subject of the real message in Mailpit ("Your Qori code is NNNNNN"), typed into `input[data-input-otp]`, submitted; the page moved to `/shared/{id}`                                                                                                                                                                           | Works on both widths                                                                            |
| The certificate                  | Marking the only Episode done showed "View your certificate"; following it landed on `/certificates/{code}` with the Series title on the page                                                                                                                                                                                                | Works                                                                                           |

**Could not verify:** that the Vimeo player inside the iframe plays anything —
`123456789` is not a real video and the assertion is on the iframe's `src`.
A run on a machine with no route to `player.vimeo.com` was not tried; the
iframe element still renders, so it should pass, and the spec says so. Nor was
a wrong or expired code tried; both are out of scope.

## Where the specification was wrong

| Section                 | Said           | Actually                                                                                                                                                                                 |
| ----------------------- | -------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Front matter of `T-119` | `blocks: none` | This task depends on `T-119`, so the board derived `blocks: T-120` and refused the mismatch until `T-119`'s line was changed. A new task's `depends:` always costs a line in the old one |

The decision "the page shows the id nowhere a person reads it" is true of
the id and was read as true of the link too; the page shows the public link
once the Series is ready. The helper is unaffected — see Found, not fixed.

## Departures

- The front-matter line in `T-119` above; a done task's file, edited for the
  one field `PROCESS.md` says to fix "when the board says so".
- `--slow`'s help text was reworded from "between browser actions" to
  "before every browser action", because the difference from `--pause` is
  the whole reason `--pause` exists.

## Found, not fixed

- `makeSeriesReady()` builds the public link from the two slugs, as the
  spec said, and the Series page shows that same link once the Series is
  ready (`#share-link`, `T-050`). Reading it from the page would also prove
  the link a creator sees is the one that works → decided: the built link and
  the shown one are the same route today; make the one-line change the next
  time a journey touches the helper
- After marking the only Episode done, the "Continue — 1. …" control
  disappears and the page offers the certificate, but the Episode row still
  reads "Done" beside a "Mark as not done" that, pressed, cannot retract the
  certificate (`ProgressService` keeps `completed_at` sticky, by design). The
  page does not say so → decided: §12's rule is right and the copy question
  belongs to a design pass, not a task; noted for the next `design-review`
  lane on state copy
- `qori:e2e` prints nothing between "Seeding database" and Playwright's first
  line for the ten seconds the server starts and Chromium launches; a person
  watching a headed run sees a blank terminal → decided: the design-review
  command has the same gap and nobody has minded; a spinner is not worth a
  task

## Evidence

None committed. The failed-step artefacts under `storage/app/e2e/` were not
produced this time, because nothing failed.

## Files

**Created:** `tests/e2e/support/steps.ts`, `tests/e2e/support/creator.ts`,
`tests/e2e/public-link.spec.ts`, `tests/e2e/invited-peer.spec.ts`.

**Modified:** `app/Console/Commands/EndToEndCommand.php`,
`tests/e2e/support/mailpit.ts`, `tests/e2e/support/person.ts`,
`tests/e2e/core-loop.spec.ts`, `docs/tinker/e2e.md`,
`docs/planning/streams/workflow.md`,
`docs/planning/tasks/T-119-the-core-loop-runs-in-a-browser-on-demand.md`.
