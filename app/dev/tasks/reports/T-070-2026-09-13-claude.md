---
task: T-070
owner: claude
date: 2026-09-13
outcome: done
gate: 659 tests, 3366 assertions
---

# T-070 — report

## Outcome

Eight starter-kit leftovers are gone: the `DatabaseSeeder` that made a "Test
User", `Icon.vue` and `HeadingSmall.vue`, the `collapsible`, `navigation-menu`
and `select` shadcn directories, and the framework's wordmark in `public/`.
The one comment that named `DatabaseSeeder` now says there is no default
seeder and points at `qori:reset`. Nothing else in the tree changed, and
nothing that remains under `resources/js/components` lacks an importer.

## Verified

| What                     | How                                                                                                                                                                                 | Result                                                                                              |
| ------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------- |
| `composer ci:check`      | Clean tree, `composer dump-autoload` and `phpstan clear-result-cache` first                                                                                                         | Green: 659 tests, 3366 assertions                                                                   |
| `vue-tsc` and `vp check` | After the deletions                                                                                                                                                                 | Clean: nothing imported what was removed                                                            |
| The inventory            | Every component by filename, every `ui/*` directory for an importer outside itself, every page for a render by name, every factory for a use, every `public/` asset for a reference | Only the eight listed had none. Ambient `.d.ts` files and `robots.txt` have none by nature and stay |
| Browser                  | Not run                                                                                                                                                                             | Nothing rendered changed                                                                            |

**Could not verify:** a production `vite build`. The gate type-checks every
import and lints every file, which is what a missing module would fail; the
bundler was not run separately.

## Where the specification was wrong

**None.**

## Departures

None.

## Found, not fixed

- **SSR.** `package.json` has a `build:ssr` script and `config/inertia.php`
  enables SSR by default (`INERTIA_SSR_ENABLED`, true), with `bootstrap/ssr`
  ignored by Git, Vite and ESLint — and there is no `resources/js/ssr.ts` to
  build. If SSR is not part of the deploy, every Inertia response outside
  tests tries `127.0.0.1:13714` and falls back; if it is, the entry is
  missing. An owner decision, then a small task either way.
- `tests/Unit` is declared in `phpunit.xml` and empty. Harmless.

## Evidence

None needed.

## Files

**Deleted**

- `database/seeders/DatabaseSeeder.php`
- `resources/js/components/Icon.vue`, `HeadingSmall.vue`
- `resources/js/components/ui/collapsible/`, `navigation-menu/`, `select/`
- `public/logo.svg`

**Modified**

- `database/seeders/DesignReviewSeeder.php`
- `docs/planning/streams/operations.md`
