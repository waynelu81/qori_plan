---
task: T-067
owner: claude
date: 2026-09-13
outcome: done
gate: 644 tests, 3256 assertions
---

# T-067 — report

## Outcome

Integrations are a settings page. An owner's sidebar offers "Plan and billing"
and "Settings"; Settings lands on a Group settings layout with a left
sub-navigation of Integrations and, when the plan allows it, Vocabulary. The
Integrations page is the Stripe page as `T-064` left it, retitled "Getting
paid", with the same states, the same connect form and the same disconnect.
Vocabulary sits beside it in the same layout with the one heading the layout
provides. "Payments" is gone from the sidebar, the old address answers 404,
and coming back from Stripe, disconnecting and the Series form's "connect
payouts" link all point at the new page.

## Verified

| What                      | How                                                                               | Result                                                                                                                                                                                |
| ------------------------- | --------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `composer ci:check`       | Clean tree, `phpstan clear-result-cache` first                                    | Green: 644 tests, 3256 assertions. The one failure on that run was the board, stale because this task was `doing`; regenerated after                                                  |
| Integrations, browser     | Signed in as Rita, opened Harbour Lane Studio's Settings                          | Sidebar shows Plan and billing and Settings, no Payments. "Group settings" header, sub-nav Integrations and Vocabulary, "Getting paid" with the Connected badge and Disconnect Stripe |
| Vocabulary, browser       | Clicked Vocabulary in the sub-nav                                                 | Same layout, one heading, the page's own line as a paragraph above its panels                                                                                                         |
| The old address           | Opened `/g/harbour-lane-studio/payouts`                                           | Qori's 404 page                                                                                                                                                                       |
| The route name everywhere | `grep` for `share.payouts.show`, `share/Payouts`, `share/Vocabulary`, `/payouts'` | Nothing left outside the generated route helpers, which regenerate                                                                                                                    |
| Light theme, mobile width | Not run                                                                           | The layout is the user-settings layout's twin, which has had both passes; the sub-nav stacks above the content under `lg` the same way                                                |

**Could not verify:** the screenshot run's two renamed shots, for the reason
`T-066`'s report gives. The vocabulary sub-nav item's plan gate was checked on
a Pro Group only; a Free Group hides it by the same shared prop the sidebar
gated on before, and no test asserts the template branch, which is the limit
`IntegrationsPageTest` states for itself.

## Where the specification was wrong

**None.**

## Departures

**The page content is full width**, not capped at the user-settings layout's
`max-w-2xl`. The facts list and the disconnect dialog were laid out for the
page's previous width, and narrowing them was a design change this task did
not ask for.

**The Vocabulary page keeps its explanatory line** as a muted paragraph above
its panels rather than losing it with its `PageHeader`. The line names the
Group and says who sees the words, which the layout's header does not.

## Found, not fixed

- `docs/flows/checkout.md` still said `/w/{group}/payouts`, the prefix from
  before the domain rename. Fixed in passing since the line was being
  rewritten; the rest of that file may carry the same.
- The Integrations page's own English (the facts list, the state sentences,
  the section title) stays inline, as the task said. `T-062` and `T-063` add
  theirs through lang and leave the rest for §13.

## Evidence

Three screenshots in the session, not committed: the Integrations page under
Settings, the Vocabulary page in the same layout, and the 404 on the old
address. Described in the table above.

## Files

**Created**

- `app/Http/Controllers/Share/IntegrationsController.php`
- `resources/js/layouts/share-settings/Layout.vue`
- `docs/planning/tasks/reports/T-067-2026-09-13-claude.md`

**Moved**

- `resources/js/pages/share/Payouts.vue` → `resources/js/pages/share/settings/Integrations.vue`
- `resources/js/pages/share/Vocabulary.vue` → `resources/js/pages/share/settings/Vocabulary.vue`
- `tests/Feature/Share/PayoutsPageTest.php` → `tests/Feature/Share/IntegrationsPageTest.php`

**Modified**

- `routes/share.php`
- `app/Http/Controllers/Share/PayoutsController.php`, `VocabularyController.php`, `SeriesController.php`
- `app/Console/Commands/DesignReviewCommand.php`
- `resources/js/components/AppSidebar.vue`
- `tests/Feature/Share/PayoutsReturnTest.php`, `PayoutsDisconnectTest.php`, `tests/Feature/Series/SeriesPriceTest.php`
- `docs/flows/checkout.md`
