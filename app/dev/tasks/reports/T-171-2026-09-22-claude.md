---
task: T-171
owner: claude
date: 2026-09-22
outcome: done
gate: 980 tests, 4734 assertions
---

# T-171 — report

## Outcome

The Episode form no longer offers Dropbox, which no save could accept. It lists
what the server says each kind can use: File offers Google Drive and Qori
storage, Video offers Vimeo and Google Drive, Audio offers Google Drive, and a
live session offers Zoom, Teams or another meeting link. When a Dropbox
connector is bound (`T-096`), Dropbox comes back with no change to the form.

## Verified

| What | How | Result |
| --- | --- | --- |
| `composer ci:check` | From a clean tree, after the code commit | Pint, the frontend checks, vue-tsc, PHPStan and 980 tests, 4734 assertions, pass (1 risky, the existing one in the rule tests) |
| The defect actually failed — how | The page before the change | `share/series/Show.vue` listed `dropbox` for File, Video and Audio, and `EpisodeServiceTest` already showed its save refused with `provider_not_available` |
| State-specific browser walk | Built-in browser as Rita on "The Quiet Handover", each kind chosen in turn | audio: Google Drive; file: Google Drive, Qori storage; live: Zoom, Teams, "Another meeting link — Meet or anything else"; video: Vimeo, Google Drive. No Dropbox |
| The tinker line | `offeredProviders(EpisodeType::Video)` | `["vimeo","google_drive"]` |

**Could not verify:** nothing further; no kind is empty today, so the form
dropping an empty kind is tested only by reading.

## Where the specification was wrong

- The first-share journey said Audio's only choice was Dropbox. Google Drive
  joined it with `T-159`; after this Audio offers Google Drive alone.
- `docs/flows/series.md`'s provider table and `docs/flows/storage.md`'s
  snippet still listed the rule before Google Drive; both are current.

## Departures

None.

## Found, not fixed

None.

## Evidence

None kept; the walk's lists are quoted above.

## Files

**Modified:** `app/Services/EpisodeService.php`,
`app/Http/Controllers/Share/SeriesController.php`,
`resources/js/pages/share/series/Show.vue`, `docs/flows/series.md`,
`docs/flows/storage.md`, `docs/tinker/series.md`,
`tests/Feature/Share/EpisodeServiceTest.php`,
`tests/Feature/Share/DriveEpisodeTest.php`.
