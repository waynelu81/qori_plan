---
task: T-159
owner: claude
date: 2026-09-21
outcome: done
gate: 915 tests, 4395 assertions
---

# T-159 — report

## Outcome

Google Drive is on the page. Setup part 3 links to it, Integrations offers
Connect Google Drive, and the Episode form leads with a Google Drive choice
that opens Google's Picker with the owner's fresh token and saves the picked
file's id, name and type. Steps 6 and 9 of the first-share journey work in
code; walking them needs the owner's Google sign-in.

## Verified

| What | How | Result |
| --- | --- | --- |
| `composer ci:check` | From the working tree | Pint, PHPStan and 915 tests pass (1 risky, the existing one in the rule tests) |
| The defect actually failed — how | 21 September walk, before the change | Setup part 3 offered nothing; the Episode form had no Drive choice |
| State-specific browser walk | Built-in browser, desktop width, two test creators | Episode form: Google Drive first for File, "Connect your Google Drive first…" and a link to Integrations when unconnected. Integrations: the section with the per-file description and Connect Google Drive. Setup part 3 for a creator mid-setup: the Google Drive line and "Connect an account" |
| The Picker's token | `DriveEpisodeTest` | Owner only, null without a live connection, renewed when about to lapse |

**Could not verify:** the Picker opening and a real file being picked. It
needs a Google sign-in, which an agent does not do, and the redirect URI and
test users the journey asks the owner for. The Picker's JavaScript is typed
against Google's documented API and has not run against it.

## Where the specification was wrong

- **Switching the section on exposed copy written for the folder design.**
  `T-044`'s Google Drive lines described one folder per Series and a sweep
  that lets people in later; `D-036` replaced the folder with per-file grants
  and `D-040` removed the sweep. The description, both tier recommendations,
  the `folder`, `shortcuts`, `limited_access` and `updating` limits, the
  shared-drive line and the disconnect and account-change sentences were
  rewritten to what is true now; `folder` became `one_file` in
  `ProviderSections::ESSENTIAL` and its test, and `limited_access` went, as it
  only ever applied to a folder. The `accounts` line promised a confirmation
  step that is `T-092`'s and not built; it now says the Qori address is the one
  that needs a Google account.
- **One test enumerated the account-bound providers by hand**
  (`EpisodeServiceTest::test_only_an_account_bound_provider_needs_a_connection`)
  and now lists Google Drive beside Dropbox.

## Departures

- `lang/en/connections.php`, `app/Support/ProviderSections.php`,
  `tests/Feature/Share/IntegrationsProvidersTest.php` — the copy above.
- `tests/Feature/Share/EpisodeServiceTest.php` — the enumeration above.
- `docs/flows/onboarding.md` — part three's link is no longer hypothetical.
- `.env.example` — `GOOGLE_API_KEY` and `GOOGLE_PROJECT_NUMBER`.
- Three test docblocks that said the connector was tagged only per case.

## Found, not fixed

- Setup part 3 sends the creator to Integrations to connect rather than
  straight to Google, because the tier choice lives there; one extra page in
  the journey's step 6. → decided: kept, one door for the connect (T-044)
- A second creator registered as "Walk Second" got `walks-group-2`: the
  generated name collides between strangers, which makes the slug finding at
  step 4 worse. → T-161
- Peer-facing copy at Open will need to name Google to say which account is
  needed; `D-035` asks for the owner's word first. → T-160

## Evidence

None committed. The browser checks are described above in words.

## Files

**Created:** `resources/js/components/series/DrivePicker.vue`,
`tests/Feature/Share/DriveEpisodeTest.php`.

**Modified:** `app/Providers/IntegrationServiceProvider.php`,
`app/Enums/EpisodeProvider.php`, `app/Enums/EpisodeType.php`,
`app/Http/Requests/Share/StoreEpisodeRequest.php`,
`app/Http/Controllers/Share/SeriesController.php`, `config/services.php`,
`.env.example`, `lang/en/series.php`, `lang/en/connections.php`,
`app/Support/ProviderSections.php`, `resources/js/pages/share/series/Show.vue`,
`docs/flows/storage.md`, `docs/flows/onboarding.md`,
`tests/Feature/Share/EpisodeServiceTest.php`,
`tests/Feature/Share/IntegrationsProvidersTest.php`,
`tests/Feature/Share/ConnectionsConnectTest.php`,
`tests/Feature/Share/ConnectionAccountChangeTest.php`,
`tests/Feature/Console/RefreshConnectionsCommandTest.php`.
