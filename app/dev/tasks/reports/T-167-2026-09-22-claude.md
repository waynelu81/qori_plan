---
task: T-167
owner: claude
date: 2026-09-22
outcome: done
gate: 999 tests, 4836 assertions
---

# T-167 — report

## Outcome

Qori's plan prices have one place that says what currency staff set them in,
`qori.billing.currency` (USD), separate from `qori.payments.default_currency`,
the default for a Series on the creator's own Stripe account. The design
fixtures now quote the planned prices, US$39 Start and US$99 Pro, instead of
the $19 and $49 placeholders, so a design review shows what a creator will be
asked to pay.

## Verified

| What | How | Result |
| --- | --- | --- |
| `composer ci:check` | From a clean worktree, on the merge of this branch with `main` at `f57fb03`, test database `qori_wt1_testing` | Pint, the frontend checks, vue-tsc, PHPStan and 999 tests, 4836 assertions, pass (1 risky, the existing one), on `d923cb8` |
| The defect actually failed — how | Not run before the change. `PricingSeeder` wrote 1900 and 4900 in a literal `'USD'`, which `test_the_design_prices_are_the_planned_usd_prices` rejects | — |
| The seeded prices | `PricingModelTest`, `ResetDatabaseCommandTest` and `DesignReviewFixtureTest` in the worktree | 29 tests pass |

**Could not verify:** the pricing and billing pages in a browser with the new
fixtures. Nothing on either page changed except the figure.

## Where the specification was wrong

None.

## Departures

- A short comment block above the new `billing` section of `config/qori.php`
  says why it is apart from `payments`. The spec gave only the key's own
  comment.

## Found, not fixed

- `SubscriptionPriceFactory` still defaults to 1900 USD. It is a test default,
  not a fixture anyone reads as a price → decided: nothing reads it as a planned price.

## Evidence

None kept.

## Files

**Modified:** `config/qori.php`, `database/seeders/PricingSeeder.php`,
`tests/Feature/Checkout/PricingModelTest.php`.
