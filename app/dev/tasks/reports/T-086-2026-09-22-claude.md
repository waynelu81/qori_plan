---
task: T-086
owner: claude
date: 2026-09-22
outcome: done
gate: 987 tests, 4802 assertions
---

# T-086 — report

## Outcome

The dashboard's name card stops asking a named Group for a name. Before a name
is chosen it reads "Name your Group" and says the name is still the one Qori
made up; once one is chosen it reads "Rename your Group" and says where the
name appears. Both come from lang, in the Group's own nouns.

## Verified

| What | How | Result |
| --- | --- | --- |
| `composer ci:check` | From a clean tree, after the three rough-edge commits | Pint, the frontend checks, vue-tsc, PHPStan and 987 tests, 4802 assertions, pass (1 risky, the existing one in the rule tests), on `250a734` |
| The defect actually failed — how | `Dashboard.vue` before the change | The title was `Name your ${noun('group')}` in every state |
| State-specific browser walk | Built-in browser as Rita, whose Group is named | The card's heading reads "Rename your Group" |

**Could not verify:** an unnamed Group's dashboard in a browser — its owner is
sent to setup; the admin's view is tested.

## Where the specification was wrong

None. The draft's two bullets are answered under Decisions: "Rename your
:group", and the description keeps both branches.

## Departures

None.

## Found, not fixed

None.

## Evidence

None kept.

## Files

**Modified:** `app/Http/Controllers/Share/DashboardController.php`,
`resources/js/pages/share/Dashboard.vue`, `lang/en/groups.php`,
`tests/Feature/Share/OnboardingHomeTest.php`.
