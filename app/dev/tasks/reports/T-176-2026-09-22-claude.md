---
task: T-176
owner: claude
date: 2026-09-22
outcome: done
gate: 990 tests, 4811 assertions
---

# T-176 — report

## Outcome

After Disconnect, an owner whose account Stripe would not deauthorize — one
Qori made on Accounts v2 before `D-023` — reads "Stripe is disconnected from
Qori, but the account itself stays open at Stripe", and where anything in it
is, instead of a plain "Stripe is disconnected". An account the last read had
already found unreachable is only said to be disconnected, because "stays
open" would not be true of it.

## Verified

| What | How | Result |
| --- | --- | --- |
| `composer ci:check` | From a clean tree, on `be22f91` (with `T-177`) | Pint, the frontend checks, vue-tsc, PHPStan and 990 tests, 4811 assertions, pass (1 risky, the existing one in the rule tests) |
| The defect actually failed — how | `PaymentsController::disconnect()` before the change | One toast, "Stripe is disconnected", whatever Stripe answered |
| The kept-open toast | `test_disconnect_lets_go_when_stripe_will_not_deauthorize`, with Stripe's observed 400 `v2_account_disconnection_unsupported` | The info toast with both lines, the id cleared |
| An unreachable account | `test_disconnecting_an_unreachable_account_says_only_that_it_is_disconnected` | "Stripe is disconnected" |

**Could not verify:** in a browser — the only such account on the platform is
the design-review Group's deleted one, which reads unreachable and so gets the
plain toast (walked with `T-163`).

## Where the specification was wrong

None.

## Departures

None.

## Found, not fixed

None.

## Evidence

None.

## Files

**Modified:** `app/Integrations/Contracts/SellsSeries.php`,
`app/Integrations/Stripe/Connect.php`, `app/Services/PaymentsService.php`,
`app/Http/Controllers/Share/PaymentsController.php`, `lang/en/payments.php`,
`docs/flows/billing.md`, `tests/Feature/Share/PaymentsOauthTest.php`.
