---
task: T-177
owner: claude
date: 2026-09-22
outcome: done
gate: 990 tests, 4811 assertions
---

# T-177 — report

## Outcome

Naming the Group in setup now gives it the URL of its name: "Walk Studio"
lives at `walk-studio`, not the `walks-group` Qori made from the name nobody
chose — while the Group has never published a Series, so no public link can
be out there. A URL another Group holds gets the next free one. Renaming from
the dashboard never moves the URL, as before.

## Verified

| What | How | Result |
| --- | --- | --- |
| `composer ci:check` | From a clean tree, on `be22f91` (with `T-176`) | Pint, the frontend checks, vue-tsc, PHPStan and 990 tests, 4811 assertions, pass (1 risky, the existing one in the rule tests) |
| The defect actually failed — how | The 21 September walk and `GroupService::rename()` | "Walk Studio" kept `walks-group` in every URL |
| The recipe | `nameInSetup()` then `rename()` on the design-review `sages-group`, inside a rolled-back transaction | `sage-studio`, then unchanged, and `sages-group` after the rollback |
| The journeys | `php artisan qori:e2e` on `cfc6b8c`, after `T-178` | All four pass in 2.3 minutes, the core loop and the first share among them. Both register a creator and name the Group in setup part one, and the Series links they then follow are built from the URL read back afterwards |

**Could not verify:** setup part one in a browser — the agent does not type a
new creator's password; the e2e journeys register one and name the Group
there.

## Where the specification was wrong

None.

## Departures

- `tests/e2e/support/creator.ts` reads the slug again after naming, and checks
  it moved.

## Found, not fixed

None.

## Evidence

None.

## Files

**Modified:** `app/Services/GroupService.php`,
`app/Http/Controllers/Share/GroupController.php`, `docs/flows/onboarding.md`,
`docs/tinker/groups.md`, `tests/Feature/Share/SetupTest.php`,
`tests/Feature/Share/SetupStepsTest.php`, `tests/e2e/support/creator.ts`.
