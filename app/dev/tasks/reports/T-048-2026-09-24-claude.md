---
task: T-048
owner: claude
date: 2026-09-24
outcome: done
gate: 1148 tests, 5700 assertions
---

# T-048 — report

## Outcome

The receiving home's one way into sharing now reads "Set up your own Group and
publish Series to the people you already know.", both nouns from the
vocabulary, with no article before either. A new `ProductNounsTest` holds
every page to it: a lowercase Group, Series, Episode or Peer in a template's
prose fails, and so does a page calling the Group a school or a workspace.
Its first run named four more pages, all fixed: the certificate page, the new
Series form (its summary placeholder and hours help), the Episode form's
preview box, and the payments card on Integrations.

## Verified

| What                             | How | Result |
| -------------------------------- | --- | ------ |
| `composer ci:check`              | Steps one by one: `vp check --fix`, `pint --test`, `vue-tsc --noEmit`, PHPStan, and the suite on `qori_wt1_testing` | Green: 1,148 tests, 5,700 assertions. One risky, the one `T-155` reported |
| The change actually did something — how | `ProductNounsTest` before any fix | Both cases failed: 7 lowercase nouns on 6 lines in 5 files, and the dashboard's "school". The staff console's 5 and the two allowed "school" lines did not fail it |
| State-specific browser walk      | At 375px, a receiving-only account signed in with `qori:magic-link`: the receiving home's panel. Then Start sharing, the first-Series screen, and a throwaway Series: the Episode form's "Let anyone preview this Episode"; the Group put on a paid plan so the new Series form shows: "What your Peers will learn"; Integrations with no Stripe account: "Connect a Stripe account to charge for your Series." | Pass. The account, its Group and Series were deleted afterwards |
| `php artisan qori:e2e`           | `--database=qori_wt1_testing --viewport=both`, because the receiving home is one of the first screens | 9 passed, 1 skipped (first-share runs desktop only, by design), 3.7 minutes |

**Could not verify:** in a browser, the certificate page and the new Series
form's hours help, both switched off with certificates (`T-158`), and
Integrations' "You're set up" line, which needs a Group whose Stripe account
can take payments. All three compile, and the test reads them.

## Where the specification was wrong

| Section | Said | Actually |
| ------- | ---- | -------- |
| Acceptance | "Board regenerated (`php artisan qori:tasks`)" | The plan moved to `qori-plan` on 21 September; `bin/tasks --check` there. The closing lines were brought up to date (Re-scope log). |
| Tests, case 2 | "the word, and the other names this product has been called in drafts", with no exceptions | "School" stands legitimately in `Welcome.vue`, saying Qori is "not a school" (`T-190`), and in `Register.vue`'s share hint, which `T-108` rewrites with the owner's copy. The test carries an allow-list, each entry the file, the phrase on the matching line and the reason; `T-108` deletes its entry. |
| Tests, case 1 | walks `resources/js` | Five of the first run's 14 hits were the staff console, which reads across Groups for Qori's staff and has no Group's vocabulary to apply. `resources/js/pages/admin/` is excluded with that reason. |

## Departures

- **Four files the Files table could not name**, fixed because the test named
  them, under **Added during execution**: `public/Certificate.vue`,
  `share/series/Index.vue`, `share/series/Show.vue` and
  `share/settings/Integrations.vue`.
- **Two fixups retired with them:** the new Series form's "your peers", twice
  (one is a placeholder attribute the test does not read, fixed with its
  sentence's twin), and the Episode form's "this episode".
- **Integrations' "Peers can buy your series"** also had "Peers" spelled by
  hand, capitalised, so the test did not name it; it is in the sentence being
  fixed and now comes from `plural('peer')`.

## Found, not fixed

- Integrations still says "Connect a Stripe account to charge for your
  Series"; the owner asked on 23 September for "charge" out of Qori's copy,
  and this task changed only the noun → decided: fixup, one line in
  `fixups.md`

## Evidence

None saved. A screenshot of the receiving home's panel at 375px was taken in
the session's browser pane.

## Files

**Created:** `tests/Feature/Design/ProductNounsTest.php`.

**Modified:** `resources/js/pages/Dashboard.vue`,
`resources/js/pages/public/Certificate.vue`,
`resources/js/pages/share/series/Index.vue`,
`resources/js/pages/share/series/Show.vue`,
`resources/js/pages/share/settings/Integrations.vue`. In `qori-plan`: this
task, its report, `fixups.md` (two lines out, one in), the language stream's
state, and `T-108`'s open bullet on the order, answered.
