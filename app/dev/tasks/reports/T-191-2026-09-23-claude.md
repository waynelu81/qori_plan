---
task: T-191
owner: claude
date: 2026-09-23
outcome: done
gate: 1130 tests, 5574 assertions
---

# T-191 — report

## Outcome

The first three next actions on a creator's dashboard, the price field's help
and the invitation page's intro now say the same thing the home page and the
first-Series screen say: start with what people already ask you, one Episode
is enough, a price easy to say yes to that can rise later, and the first few
people you would tell. The first-Episode line no longer promises "text" and "a
link", which are not Episode kinds.

## Verified

| What                             | How | Result |
| -------------------------------- | --- | ------ |
| `composer ci:check`              | Steps one by one, the suite at `HEAD` with `T-190` and `T-028` | Green: 1,130 tests, 5,574 assertions, on `qori_wt2_testing`. A first run on `qori_wt3_testing` errored in 59 tests with missing tables: another suite shared that database. One risky, the one `T-155` reported |
| The change actually did something — how | The five lines read the old English before, and a grep found no test pinning any of them; `TerminologyTest`'s article walk and `ShareDigestTest` read the new ones | Pass |
| State-specific browser walk      | Not walked: lang lines only, read by keys whose readers did not change | Not run |

**Could not verify:** the lines in place on a fresh owner's dashboard in a
browser. `T-026`'s walk passed through the first Series before this landed.

## Where the specification was wrong

None.

## Departures

None.

## Found, not fixed

None.

## Evidence

None.

## Files

**Modified:** `lang/en/share.php`, `lang/en/series.php`,
`lang/en/invitations.php`. In `qori-plan`: this task and its report.
