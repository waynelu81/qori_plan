---
task: T-155
owner: claude
date: 2026-09-22
outcome: done
gate: 1092 tests, 5356 assertions
---

# T-155 — report

## Outcome

`/privacy` and `/terms` are pages a signed-out visitor can read. The home page's
footer links to both, and they serve the privacy policy and terms drafted on 19
September. Google's consent screen has linked both addresses since the OAuth app
was published, and both answered 404; they work from the first deploy after
this. At the owner's word the documents go up as drafts. Each page opens with
"This is a draft" while its file still carries a drafting marker or a
placeholder, and the notice comes off by itself when the last one goes. What
the documents still need to be finished is release checklist, now in
`release-prerequisites.md` under "The privacy policy and the terms".

## Verified

| What                             | How                                                                                                                                                                                                                                    | Result                                        |
| -------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------- |
| `composer ci:check`              | Its steps run one by one, the suite on `qori_wt3_testing` because other sessions share this checkout: `pint --test`, `vp check`, `vue-tsc --noEmit`, PHPStan, `php artisan test`                                                     | Green: 1,092 tests, 5,356 assertions. One test is flagged risky, and not one of the eleven here: `LegalPageTest` passes under `--fail-on-risky` |
| The defect actually failed — how | `https://useqori.com/privacy` and `/terms` both answered 404 on 22 September 2026, before the change; locally neither route existed                                                                                                   | Reproduced                                    |
| State-specific browser walk      | A signed-out visitor at `localhost:8001`: both pages under the draft notice, a contents link landing on its heading, the privacy policy's ten tables, and the home page's footer; at 375px and desktop width, in dark and light     | Pass, after the two fixes under Departures    |

**Could not verify:** production. The live addresses answer 404 until this is
deployed, and nothing was pushed. Google's Branding page was not opened. A
finished document, with no notice, was checked only against fixtures, because
both real documents are drafts.

## Where the specification was wrong

| Section         | Said                                                                                         | Actually                                                                                                                                                                                                                      |
| --------------- | -------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Files, Code     | `resources/js/pages/Legal.vue`, `Inertia::render('Legal', …)`                                 | `resources/js/app.ts` gives the bare layout only to `Welcome`, `Pricing` and `pages/public/*`. As `Legal` the policy rendered inside the app's sidebar, beside "Shared with me". It is `public/Legal`.                         |
| Code            | `Str::markdown($contents, ['html_input' => 'strip', 'allow_unsafe_links' => false])`         | The privacy policy's contents are 32 links to its own headings, and CommonMark gives a heading no id, so none of them landed. `HeadingPermalinkExtension` puts an unprefixed id on each heading.                              |
| Files, Copy     | The footer labels come from `lang/en/legal.php` into `Welcome.vue`                            | The home page is a `Route::inertia` with no controller to pass them. They are a shared prop, which touched `HandleInertiaRequests` and `global.d.ts` (Added during execution).                                                |
| Notes           | "nothing reaches the consent screen … it ends the day `T-094` lands"                          | `T-159` tagged `GoogleAccounts` as an account connector first, so the consent screen, and its two 404 links, were reachable from `T-159` on.                                                                                   |
| Acceptance      | `php artisan qori:tasks --check` passes                                                      | The board is `bin/tasks --check` in `qori-plan` since 21 September.                                                                                                                                                             |
| Acceptance      | `php artisan qori:reachability` reports no unreachable GET route                             | It reports none, and still exits 1, on two service methods this task did not touch (Found, not fixed).                                                                                                                       |

## Departures

- **The re-scope itself**, at the owner's word ("can you hook up /terms and
  /privacy from the draft first."): the drafts are served now, and a draft
  notice replaces the two tests that refused a document with a marker or a
  placeholder. Eleven tests instead of seven; the task lists them.
- **`break-words` on the document.** A bare URL as link text in the privacy
  policy (`https://security.google.com/settings/security/permissions`) made the
  page scroll sideways at 375px.
- **Three intended line breaks made hard in the drafts**, with a trailing
  backslash: "Effective date" above "Version", and the address under each
  "Contact us". CommonMark had joined each pair into one line. No wording
  changed.
- **`docs/pptcs/README.md` says more than where the copy lives.** Its opening
  now says the lawyer's review comes before Qori launches rather than before
  publication, and a marker "blocks launch" rather than being a "publish
  blocker", because the owner chose to publish the drafts. Its open question on
  where the two pages live is answered, and its 35 links follow the files.
- **Two files not in the Files table**, under **Added during execution**:
  `app/Http/Middleware/HandleInertiaRequests.php` and
  `resources/js/types/global.d.ts`.
- **The task's Before this can be ready moved rather than went.** Its bullets
  were about the documents alone, so they are release checklist in
  `release-prerequisites.md`, where step 3 of opening the live Stripe account
  now says to finish the documents first.

## Found, not fixed

- `php artisan qori:reachability` exits 1 on `AccessService::peerFor()` and
  `SuppressionService::suppress()`, public methods nothing calls from outside
  their own class; its unreachable-routes list is empty. → decided: not a task — two keywords, recorded in `fixups.md` for whoever is next in those files
- `/pricing` logs a hydration mismatch on every price, "GBP 30" from the server
  and "£30" in the browser, from `Pricing.vue` formatting with
  `Intl.NumberFormat(undefined, …)`. `/privacy` and `/terms` log none in a
  fresh tab. → T-182

## Evidence

None saved. Screenshots were taken in the session's browser pane and not kept:
the privacy policy under the notice at desktop width in dark mode, the terms and
the home page's footer at 375px, and the terms in light mode.

## Files

**Created:** `app/Http/Controllers/LegalController.php`,
`app/Support/LegalDocument.php`, `lang/en/legal.php`,
`resources/js/pages/public/Legal.vue`, `tests/Feature/Legal/LegalPageTest.php`;
`resources/legal/privacy-policy.md` and `resources/legal/terms-and-conditions.md`,
moved from `docs/pptcs/`.

**Modified:** `routes/web.php`, `resources/js/pages/Welcome.vue`,
`app/Http/Middleware/HandleInertiaRequests.php`,
`resources/js/types/global.d.ts`, `docs/pptcs/README.md`. In `qori-plan`: this
task, `release-prerequisites.md`, `fixups.md`, and one line in `T-182`.
