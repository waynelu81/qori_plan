---
task: T-175
owner: claude
date: 2026-09-22
outcome: done
gate: 987 tests, 4802 assertions
---

# T-175 — report

## Outcome

The code a Peer asks for from a Series page now says what it is for and who it
comes from: "Your code for Beginner Piano is 123456", and "You asked to get
Beginner Piano, shared by Rita's Piano. Type 123456 on its page to carry on."
A code sent from anywhere else stays plain.

## Verified

| What | How | Result |
| --- | --- | --- |
| `composer ci:check` | From a clean tree, after the three rough-edge commits | Pint, the frontend checks, vue-tsc, PHPStan and 987 tests, 4802 assertions, pass (1 risky, the existing one in the rule tests), on `250a734` |
| The defect actually failed — how | `LoginCodeNotification` before the change | Subject "Your Qori code is :code", line "Type :code on the page you came from" — no Series, no Group |
| The email itself | `test_the_code_email_names_the_series_and_who_shared_it` renders the mail the start step sent | The subject and line above, with the lifetime from `LIFETIME_MINUTES` |
| The journeys | `php artisan qori:e2e` after the gate | All four journeys pass (1.2 min): the core loop, the first share, the invited Peer and the public link — the last two read the code from Mailpit by "code" in the subject |

**Could not verify:** the email in a real inbox in a browser; the e2e journeys
read it from Mailpit.

## Where the specification was wrong

None.

## Departures

None.

## Found, not fixed

None.

## Evidence

None kept.

## Files

**Modified:** `app/Services/LoginCodeService.php`,
`app/Notifications/LoginCodeNotification.php`,
`app/Http/Controllers/SeriesAccessController.php`, `lang/en/auth.php`,
`docs/flows/auth.md`, `tests/Feature/Access/SeriesAccessCodeTest.php`.
